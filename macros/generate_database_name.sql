{% macro generate_database_name(custom_database_name=none, node=none) -%}
    {%- if custom_database_name is none -%}
        {{ return(none) }}
    {%- else -%}
        {{ return(none) }}
    {%- endif -%}
{%- endmacro %}